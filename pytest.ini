[pytest]
testpaths = tests/direct
python_files = test_*.py
addopts = -p no:hydra_pytest -q
markers =
    direct: direct-mode contract test (no simulator)
    slow: needs a live network, and value assertions wait for finalization
